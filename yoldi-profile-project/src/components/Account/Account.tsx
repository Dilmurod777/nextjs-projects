import React from 'react';

type AccountProps = {};

const Account: React.FC<AccountProps> = (props) => {
    return (
        <div>
            Account
        </div>
    );
};

export default Account;
